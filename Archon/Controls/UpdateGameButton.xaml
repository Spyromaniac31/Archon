<UserControl
    x:Class="Archon.Controls.UpdateGameButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Orientation="Horizontal">
        <winui:TeachingTip x:Name="StopServerTip" Title="Server running" Subtitle="Stop the server before updating ARK." Target="{x:Bind UpdateButton}">
            <winui:TeachingTip.IconSource>
                <winui:FontIconSource FontFamily="{StaticResource FluentUIGlyphs}" Glyph="&#xEA41;"/>
            </winui:TeachingTip.IconSource>
        </winui:TeachingTip>
        <Button Background="Transparent" x:Name="UpdateButton" Click="{x:Bind Clicked}" Margin="12" PointerEntered="UpdateButton_PointerEntered" PointerExited="UpdateButton_PointerExited">
            <Button.Content>
                <FontIcon FontSize="40" FontFamily="{StaticResource FluentUIGlyphs}" Glyph="&#xE91C;"/>
            </Button.Content>
        </Button>
        <TextBlock x:Name="Tooltip" Style="{StaticResource ButtonTooltip}" Text="Update ARK" TextAlignment="Left" VerticalAlignment="Center" Visibility="Collapsed"/>
    </StackPanel>
</UserControl>
